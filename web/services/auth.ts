
export enum Status {
  REMOVED = 0,
  ACTIVE = 1,
}

// Dockly Users
export enum DocklyUsers {
  Guests = 0,
  PaidMember = 1,
  SuperAdmin = 2,
  Developer = 3,
}

// Trial Period
export enum TrialPeriod {
  DAYS = 14,
}

